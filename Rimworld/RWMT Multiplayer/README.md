This egg installs the standalone multiplayer server for the [Zetrith's Multiplayer mod](https://github.com/rwmt/Multiplayer).

I can not guarantee that the server works well.
As of writing this there is only a beta release of the server which only works with a beta version of the mod which has to be installed manually.